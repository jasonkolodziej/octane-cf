name = "octane-app"
main = "src/server/health.ts"
compatibility_date = "2024-11-15"
compatibility_flags = []

[env.production]
name = "octane-app-prod"

# Assets configuration for static files
[assets]
binding = "ASSETS"
directory = "dist/client"

# Optional: Add KV namespace for session storage if needed
# [[kv_namespaces]]
# binding = "SESSIONS"
# id = "your-kv-namespace-id"
# preview_id = "your-preview-namespace-id"
